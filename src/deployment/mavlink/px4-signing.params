# =============================================================================
# px4-signing.params — PX4 数传链路参数模板 (task-14)
#
# 导入方式: QGroundControl → Vehicle Setup → Parameters → Tools → Load from file
#           或机载 shell: `param load /fs/microsd/px4-signing.params`
#
# -----------------------------------------------------------------------------
# ⚠ 签名参数**尚未核实**, 因此没有写进本文件的生效部分。
#
# task-14 原文给出的是 `MAV_0_SIGNING=1`。落地时无法确认这个参数在本项目
# 使用的 PX4 v1.14 上存在:
#   · 原文引用的 docs.px4.io/v1.14/.../mavlink_signing.html 打不开;
#   · 官方 "MAVLink Message Signing" 文档目前只在 `main` (v1.18 时间线)
#     路径下检索得到, v1.14 路径下没有对应页面。
#
# 按 ADR-0008 §决策-1 的标准, 这属于"没实测过"——那就不能当成配置发出去。
# 一份写着不存在的参数的 .params, `param load` 时 PX4 会**跳过未知参数并继续**,
# 于是操作者会得到一次"导入成功", 而签名根本没开。这比没有模板更糟。
#
# 上机后的核实步骤 (task-15 联调时做):
#   1. nsh> param show MAV_*        # 看有没有签名相关参数
#   2. 有 → 把参数名填进下面的"签名"段, 删掉本段警告, 并在 ADR-0010 记录结论
#   3. 没有 → v1.14 不支持签名, 按 ADR-0010 §方案-B 走链路层加密, 或升级固件
# -----------------------------------------------------------------------------
#
# 格式: <索引> <系统ID> <组件ID> <参数名> <值> <类型>   类型 6=int32, 9=float
# 索引与系统/组件 ID 由 QGC 导入时改写, 这里按惯例填 1/1。

# --- TELEM1 (3DR SiK 数传, 接 Pixhawk 6C 的 TELEM1) ---
# MAV_0_CONFIG=101: 把 MAVLink 实例 0 绑到 TELEM1
1	1	1	MAV_0_CONFIG	101	6
# MAV_0_MODE=0: Normal 模式 (常规遥测集)
1	1	1	MAV_0_MODE	0	6
# MAV_0_RATE=1200: 该链路的目标字节率 (B/s)。
# 57600 bps 的 SiK 电台实际可用带宽远低于标称, 1200 B/s 是保守值;
# 设成 0 会让 PX4 按半带宽自动推算, 在丢包链路上容易灌满队列。
1	1	1	MAV_0_RATE	1200	6
# MAV_0_FORWARD=1: 允许把消息转发到其他 MAVLink 实例 (车机 Pi ↔ 地面站)
1	1	1	MAV_0_FORWARD	1	6

# --- 签名 (待核实后启用) ---
# 参数名确认之前刻意留空。不要凭 task-14 原文补 `MAV_0_SIGNING=1`,
# 那个名字没有在任何可访问的官方文档里得到确认。
#
# 另外: 签名密钥本身**永远不写进 .params 文件**。
# .params 是明文且会被导出/贴到工单里, 密钥要走 QGC 的安全通道单独设置。
