# air_ground_com_bridge — Python dependencies
# MAVLink UDP 桥 + TCP JSON 通信桥

# MAVLink 协议解析
pymavlink>=2.4.39,<3.0.0

# 图像处理（边缘端 JPEG 压缩）
opencv-python-headless>=4.8.0,<5.0.0
Pillow>=9.0.0,<11.0.0
numpy>=1.22.0,<2.0.0

# YAML 配置解析（network.yaml）
PyYAML>=6.0,<7.0
