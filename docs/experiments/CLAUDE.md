# CLAUDE.md

> 本文件会被自动读入上下文，因此**刻意保持简短**。
> 详细交接见 **[docs/AI_HANDOFF.md](./docs/AI_HANDOFF.md)** —— 动手之前先读它。

## 项目

空地联合具身智能研究平台（无人机 + 可换底盘地面车 + 实验室服务器），
目标是 Phase 2 的 EQA 论文。Phase 0 仿真 ✅ · Phase 1 固件+部署 ✅ · Phase 2 未开始。

工作分支是 `feat/task-XX`，不是 `main`。**commit 会自动推送到 origin。**

## 动手前

```bash
make smoke-phase1          # 交付物 + 语法 + 自测 + CI 归属，不需要 ROS
```
输出里的 `跳过` **不等于通过**——它表示这台机器缺依赖，那几项没被检查。

## 七条硬规则

1. **核对现状，不要照抄任务书。** `task-NN-*.md` 写于开工之前，会过期。
   已连续三次抓到冲突（话题名、job 名、文件路径）。发现的偏差写进该任务文档
   末尾的「§与原方案的偏差」，**不改正文**。
2. **替身可以替环境（ROS/硬件/时钟），不能替被测对象。** 用
   `src/air_ground_car_bringup/test/host/ros_stub.py` 跑真节点，不要另写"模拟核心逻辑"的函数。
3. **没实测过的检查不设成阻塞门禁；实测干净了也不该留在告警态。**（ADR-0008 / ADR-0012）
4. **查不了的项报 SKIP 并说明原因。** 全仓约定：退出码 `2`=SKIP，`1`=真失败。
5. **写完守卫做一次负向测试**——测"该拦的真的拦住了"，不只测"该过的过了"。
6. **ADR 不可变**，只由新 ADR 替代。日记必须写日期。0013 已预留给超声波时序。
7. **没核实过的参数名/接口不写进生效配置**，要么留空自曝，要么标成核实清单。（ADR-0010）

## 语言

注释、文档、commit message 用中文；标识符、分支名用英文。详见 `CONVENTIONS.md` §4.2。

## 环境

`python3` 可能不在 PATH（Windows 上用 `python`）。输出中文前
`export PYTHONIOENCODING=utf-8:replace`。跑 lint 门禁前先 `git add -A`——
`git ls-files` 不含未跟踪文件，否则新文件会**没被检查却显示 0 条**。
